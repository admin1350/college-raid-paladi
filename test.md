# Технический отчёт: Проектирование дисковой подсистемы для сервера видеонаблюдения Aquarius T50 D224CF

## 📊 1. Анализ требований и приоритетов

| Аспект | Анализ и выводы |
| :--- | :--- |
| **Характер нагрузки** | **Запись**: интенсивная, последовательная, круглосуточная потоковая запись видео. **Чтение**: редкое, эпизодическое, по запросу для просмотра архивов. |
| **Приоритеты** | **1. Надёжность и отказоустойчивость**: непрерывность работы — критически важна.<br>**2. Ёмкость**: обеспечение хранения не менее 28 ТБ видеоданных.<br>**3. Скорость**: важна стабильная скорость последовательной записи.<br>**4. Стоимость**: вторичный фактор, оптимизация идёт после обеспечения надёжности. |
| **Допустимые риски** | Согласно легенде, допустима потеря части данных (записанных в момент сбоя), но система в целом должна продолжать работу. Требуется устойчивость к отказу одного или нескольких носителей. |

## 💾 2. Проектирование аппаратного RAID

### 2.1 Выбор платформы и контроллера
*   **Серверная платформа**: **Aquarius T50 D224CF** (2U, 24 слота для дисков 2.5" с горячей заменой, встроенный BMC для удалённого управления).
*   **Тип RAID-контроллера**: **Аппаратный RAID-контроллер** (например, LSI MegaRAID 9560-24i). Выбор обоснован необходимостью:
    *   Разгрузки центрального процессора от вычислений чётности.
    *   Наличия собственной энергонезависимой кэш-памяти (NVCache) с защитой от сбоев питания (BBU или Flash Backup Unit) для целостности данных.
    *   Максимальной производительности при потоковой записи.

### 2.2 Выбор уровня RAID
*   **Выбранный уровень**: **RAID 60** (чередование (RAID 0) нескольких массивов RAID 6).
*   **Обоснование**:
    1.  **Двойная отказоустойчивость в каждой группе**: каждая группа RAID 6 выдерживает одновременный отказ **ЛЮБЫХ двух дисков** в своей группе. Для системы видеонаблюдения, где перестроение массива (rebuild) массивных дисков занимает много часов, это критически снижает риск полного отказа массива при сбое второго диска в процессе восстановления.
    2.  **Высокая скорость последовательной записи**: RAID 6, несмотря на расчёт двух контрольных сумм, при использовании аппаратного контроллера с кэшем хорошо справляется с последовательной нагрузкой, а чередование (RAID 0) между группами увеличивает общую пропускную способность.
    3.  **Большая эффективная ёмкость**: обеспечивает лучший баланс между надёжностью и полезным объёмом по сравнению с RAID 1 или RAID 10 для большого количества дисков.

### 2.3 Выбор дисков и расчёт конфигурации
*   **Тип дисков**: **Жёсткие диски (HDD) SAS 12Gb/s, 1.8 ТБ**, предназначенные для работы в ЦОД (с высоким MTBF и нагрузкой на запись). Интерфейс SAS обеспечивает лучшую надёжность и полнодуплексную связь с контроллером по сравнению с SATA.
*   **Количество дисков**: **20 дисков** в основном массиве + **2 диска** в резерве (Hot-Spare) или для использования в отсеках на задней панели сервера.
*   **Расчёт полезного объёма**:
    *   Физический объём: `20 дисков * 1.8 ТБ = 36 ТБ`.
    *   Конфигурация: RAID 60, сгруппированный как **4 группы по 5 дисков в RAID 6**.
    *   Полезный объём одной группы RAID 6: `(5 дисков - 2 диска на чётность) * 1.8 ТБ = 3 * 1.8 ТБ = 5.4 ТБ`.
    *   **Общий полезный объём массива**: `4 группы * 5.4 ТБ = 21.6 ТБ`.
*   **Итог по объёму**: Заявленный требуемый объём — 28 ТБ. Рассчитанный объём в 21.6 ТБ **недостаточен**. Требуется корректировка.

### 2.4 Корректировка конфигурации под требования по ёмкости
Для достижения полезной ёмкости **свыше 28 ТБ** при сохранении уровня отказоустойчивости RAID 60:
*   **Вариант 1 (рекомендуемый)**: Использовать диски большей ёмкости.
    *   **Диски**: **20 x HDD SAS 12Gb/s, 2.4 ТБ**.
    *   **Расчёт**: `4 группы * (5-2) * 2.4 ТБ = 4 * 3 * 2.4 ТБ = 28.8 ТБ`. **Требование выполнено**.
*   **Вариант 2**: Увеличить количество дисков в группах, уменьшив количество групп (менее оптимально из-за увеличения времени перестроения).
    *   Конфигурация: RAID 60 из **2 групп по 10 дисков в RAID 6**.
    *   Используем диски по 1.8 ТБ: `2 группы * (10-2) * 1.8 ТБ = 2 * 8 * 1.8 ТБ = 28.8 ТБ`. **Требование выполнено**.

**Окончательный выбор**: **Вариант 1** (20 x SAS 2.4 ТБ, RAID 60 из 4 групп по 5 дисков), как обеспечивающий лучшее время восстановления и распределение нагрузки.

**Допустимое количество отказов**: В предложенной конфигурации **массив останется работоспособным при одновременном отказе ЛЮБЫХ двух дисков в КАЖДОЙ из групп RAID 6**. Теоретически, система может пережить до 8 одиночных отказов, если они будут грамотно распределены по разным группам.

## 🔧 3. Принципы эксплуатации

### 3.1 Порядок создания RAID-массива (имитация)
1.  **Физическая установка**: Установить все жёсткие диски в отсеки сервера.
2.  **Вход в утилиту конфигурации**: При загрузке сервера войти в интерфейс управления RAID-контроллером (часто по комбинации `Ctrl+R`).
3.  **Инициализация дисков**: В интерфейсе контроллера (например, MegaRAID BIOS Configuration Utility) убедиться, что все диски обнаружены.
4.  **Создание Virtual Drive**:
    *   Выбрать опцию создания нового массива.
    *   Выделить все 20 дисков основного массива.
    *   Выбрать уровень RAID: **RAID 60**.
    *   В расширенных настройках (Advanced) задать конфигурацию групп: **Span = 4, Drives per Span = 5**. Это создаст 4 группы RAID 6 по 5 дисков, объединённых в RAID 0.
    *   Задать параметры: размер полосы (Stripe Size) - оптимально 256KB или 512KB для потокового видео, политику чтения/записи (Read/Write Policy) - оставить по умолчанию или включить `WriteBack` при наличии защищённого кэша.
    *   Начать процесс инициализации (фоновое медленное построение) или инициализацию с очисткой (медленнее, но надёжнее).
5.  **Настройка Hot-Spare**: После создания основного массива назначить один или два оставшихся диска в качестве глобальных горячих резервов (Global Hot-Spare).
6.  **Завершение**: Сохранить конфигурацию, выйти из утилиты. После загрузки ОС массив будет виден как один логический диск.

### 3.2 Сценарий отказа диска и процесс восстановления (Rebuild)
1.  **Обнаружение**: Контроллер, BMC (через IPMI) и операционная система (если установлен мониторинг) генерируют тревоги: звуковые, световую индикацию на передней панели (мигающий amber LED на неисправном диске), email/SMS-уведомления.
2.  **Подготовка к замене**: Администратор получает уведомление, идентифицирует слот неисправного диска по логическому номеру (из утилиты) или свечению индикатора.
3.  **Горячая замена**: **Без выключения сервера** извлекается неисправный диск и на его место вставляется новый аналогичный диск (той же или большей ёмкости, SAS-интерфейса).
4.  **Автоматическое восстановление**:
    *   RAID-контроллер автоматически распознает новый диск.
    *   Если настроен Hot-Spare, он немедленно начинает использоваться для восстановления. Новый установленный диск затем становится новым Hot-Spare.
    *   Если Hot-Spare не настроен, контроллер автоматически начинает процесс перестроения (**Rebuild**) данных на новый диск, используя информацию о чётности с остальных дисков в группе.
5.  **Мониторинг восстановления**: Процесс можно отслеживать через утилиту контроллера или BMC. **Производительность массива во время Rebuild может быть снижена**. После завершения система возвращается в штатный режим, все уведомления сбрасываются.

### 3.3 Типовые ошибки администратора
1.  **Игнорирование предупреждений**: Не реагировать на сообщения о предотказном состоянии диска (SMART errors) или деградации массива.
2.  **Неоднородность дисков**: Использование в одном массиве дисков разных моделей, производителей или, что критично, ёмкостей (приводит к потере части ёмкости больших дисков).
3.  **Прерывание процесса Rebuild**: Намеренная или случайная перезагрузка сервера во время перестроения массива. Это может привести к полной потере данных.
4.  **Отсутствие тестов отказоустойчивости**: Никогда не проверять, как система ведёт себя при имитации отказа (например, программном извлечении диска в тестовой среде).
5.  **Пренебрежение резервированием инфраструктуры**: Установка отказоустойчивого RAID при отсутствии ИБП, что ведёт к краху всей системы при скачке напряжения.
6.  **Отсутствие резервных копий конфигурации контроллера**: При выходе контроллера из строя и замене на новый, без бэкапа конфигурации можно столкнуться с длительным процессом импорта или, в худшем случае, потерей доступа к данным.

## 🗂️ 4. Схема дисковой подсистемы

```mermaid
block-beta
columns 8

block:group:Оптимальная конфигурация для видеонаблюдения Aquarius T50
    block:group:Два запасных диска (“Hot Spare”)
        space
        diskSpare1["HDD 2.4TB<br>(Spare 1)"]
        diskSpare2["HDD 2.4TB<br>(Spare 2)"]
        space
    end
    block:group:Основной массив RAID 60 (4 группы по 5 дисков)
        block:group:Группа 1 (RAID 6)
            disk1_1["HDD 2.4TB"]
            disk1_2["HDD 2.4TB"]
            disk1_3["HDD 2.4TB"]
            disk1_4["HDD 2.4TB"]
            disk1_5["HDD 2.4TB"]
        end
        block:group:Группа 2 (RAID 6)
            disk2_1["HDD 2.4TB"]
            disk2_2["HDD 2.4TB"]
            disk2_3["HDD 2.4TB"]
            disk2_4["HDD 2.4TB"]
            disk2_5["HDD 2.4TB"]
        end
        block:group:Группа 3 (RAID 6)
            disk3_1["HDD 2.4TB"]
            disk3_2["HDD 2.4TB"]
            disk3_3["HDD 2.4TB"]
            disk3_4["HDD 2.4TB"]
            disk3_5["HDD 2.4TB"]
        end
        block:group:Группа 4 (RAID 6)
            disk4_1["HDD 2.4TB"]
            disk4_2["HDD 2.4TB"]
            disk4_3["HDD 2.4TB"]
            disk4_4["HDD 2.4TB"]
            disk4_5["HDD 2.4TB"]
        end
    end
end